<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Product</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-purple-200 via-pink-100 to-yellow-100 min-h-screen flex items-center justify-center">

    <div class="w-full max-w-2xl bg-gradient-to-tr from-white via-gray-50 to-white rounded-3xl shadow-2xl p-10 hover:shadow-xl transition-shadow duration-300">
        <h1 class="text-3xl font-extrabold text-gray-800 mb-8 text-center">Update Product</h1>

        <form action="/update" method="post" class="space-y-6">
            <input type="hidden" name="updateId" value="<%= updateId %>">

            <!-- Product Name -->
            <div>
                <label for="productName" class="block text-sm font-medium text-gray-700">Product Name</label>
                <input type="text" id="productName" name="productName" value="<%= updateData.productName %>"
                    class="mt-2 block w-full rounded-2xl border border-gray-300 shadow-sm focus:ring-2 focus:ring-pink-400 focus:border-pink-400 p-4 placeholder-gray-400 transition"
                    placeholder="Enter product name">
            </div>

            <!-- Product Price -->
            <div>
                <label for="productPrice" class="block text-sm font-medium text-gray-700">Product Price</label>
                <input type="text" id="productPrice" name="productPrice" value="<%= updateData.productPrice %>"
                    class="mt-2 block w-full rounded-2xl border border-gray-300 shadow-sm focus:ring-2 focus:ring-pink-400 focus:border-pink-400 p-4 placeholder-gray-400 transition"
                    placeholder="Enter product price">
            </div>

            <!-- Product Image -->
            <div>
                <label for="productImg" class="block text-sm font-medium text-gray-700">Product Image URL</label>
                <input type="text" id="productImg" name="productImg" value="<%= updateData.productImg %>"
                    class="mt-2 block w-full rounded-2xl border border-gray-300 shadow-sm focus:ring-2 focus:ring-pink-400 focus:border-pink-400 p-4 placeholder-gray-400 transition"
                    placeholder="Enter image URL">
            </div>

            <!-- Category -->
            <div>
                <label for="category" class="block text-sm font-medium text-gray-700">Category</label>
                <select id="category" name="category"
                    class="mt-2 block w-full rounded-2xl border border-gray-300 shadow-sm focus:ring-2 focus:ring-pink-400 focus:border-pink-400 p-4 transition">
                    <option value="">All Product</option>
                    <option value="sports" <%=updateData.category==="sports" ? "selected" : "" %>>Sports</option>
                    <option value="cloth" <%=updateData.category==="cloth" ? "selected" : "" %>>Clothes</option>
                    <option value="electronics" <%=updateData.category==="electronics" ? "selected" : "" %>>Electronics & Gadgets</option>
                    <option value="beauty" <%=updateData.category==="beauty" ? "selected" : "" %>>Beauty & Personal Care</option>
                    <option value="books" <%=updateData.category==="books" ? "selected" : "" %>>Books & Stationery</option>
                </select>
            </div>

            <!-- Submit Button -->
            <div class="flex justify-end">
                <button type="submit"
                    class="px-8 py-4 rounded-2xl bg-pink-400 text-white font-bold shadow-lg hover:bg-pink-500 hover:shadow-xl transform hover:-translate-y-1 transition-all duration-300">
                    Update
                </button>
            </div>
        </form>
    </div>

</body>

</html>
